export const goToPilots = (history) => {
    history.push("/pilots")
}

export const goToStarships = (history) => {
    history.push("/starships");
}

export const goToHome = (history) => {
    history.push('/')
}